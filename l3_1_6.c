#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int s,i,n;
int main()
{
  a)int i = 1;      while (i < 40) {          printf("*");          i++;    39  }
(b)  int i = 1;      do      {          printf ("*");          i += 3;     14 } while (i != 40);(c)  for (int i = 1; i != 40; i += 2)     infinit     printf ("*");(d)  for (int i = 1; i <= 40; i += 15)          for (int j = 1; j <= i; j++)     48         printf ("*");
   return 0;
}
