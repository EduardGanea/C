#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int s,i,n;
int main()
{
    int sum = 0;     int nextValue = 1;     for (int i = 0; i<=n; i++)  {         sum +=nextValue;         nextValue *=3;}
    (b)   int sum = 0;     int nextValue = 1;     for (int i = 0;i<=5;i++)  {         sum +=nextValue;         nextValue *=3;}
   return 0;
}
